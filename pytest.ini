[pytest]
addopts = -v --tb=short
markers =
    mps: mark test to run on MPS device
    slow: mark test as slow
    fast: mark test as fast
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*